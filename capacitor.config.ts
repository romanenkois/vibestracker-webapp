import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vibes.tracker',
  appName: 'vibes tracker',
  webDir: 'dist/vibestracker-webapp/browser',
};

export default config;
